info:
  component: Zuul Server

endpoints:
  restart:
    enabled: true
  shutdown:
    enabled: true
  health:
    sensitive: false

zuul:
  routes:
    zuulservice:
      path: /**
      url: http://localhost:8081

eureka:
  client:
    enabled: false

server:
  port: 8080
